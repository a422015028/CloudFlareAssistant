### **v7.6.8 更新日志**

#### 新增：Pages 服务绑定管理
- 新增 Pages 项目服务绑定配置功能，支持将其他 Worker 脚本绑定到 Pages Functions
- 完整弹窗配置流程：选择目标 Worker 脚本、输入绑定名称，一键添加服务绑定
- 支持查看、添加、删除服务绑定，应用配置时自动移除已删除项
- 服务环境默认 production，与 Cloudflare Dashboard 行为一致
- 生产/预览环境配置相互独立，随当前环境上下文自动应用

#### 新增：Worker 服务绑定管理
- 新增 Worker 脚本服务绑定配置功能，支持绑定其他 Worker 脚本互相调用
- 与 Pages 一致的弹窗配置流程，自动加载可绑定的 Worker 脚本列表
- 更新绑定时自动保留 KV、R2、D1 等其他绑定与兼容性日期，无需重新上传脚本代码

本次更新聚焦 Pages 与 Worker 服务绑定管理功能上线，建议所有用户升级。
