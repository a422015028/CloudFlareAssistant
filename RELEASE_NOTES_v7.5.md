注：pages部署项目 目前未修复，不要使用该功能部署项目！\
\
**1. 🚀 新增：隧道令牌交互功能（`TunnelsFragment`）**
========================================

- **调用 API**：`viewModel.getTunnelToken` 获取令牌。
- **展示内容**：令牌明文及完整命令字符串。
- **交互操作**：
  - **显示/隐藏**令牌（切换可见性）。
  - **复制命令**至剪贴板（一键复制）。
- **清理逻辑**：删除隧道时自动隐藏令牌区域，避免残留信息。

***

## **2. ✨ 升级：脚本编辑器 → Monaco Editor**

替换原编辑器，提供类 Cloudflare Dashboard 的开发体验。

**特性**

**说明**

**智能补全**

支持 `Request`、`Response`、`KV`、`R2`、`D1`、`Durable Object` 等 Workers 全局类型

**语法诊断**

实时 TypeScript 级别错误检查（语法 + 类型）

**暗色主题**

模仿 Cloudflare Dashboard 暗色样式

**离线可用**

所有资源本地打包，无需网络

**响应式布局**

自动适配屏幕尺寸，键盘弹出时调整视口

**搜索高亮**

支持查找、下一个/上一个、清除高亮
