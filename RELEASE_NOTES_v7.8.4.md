## **v7.8.4 更新日志**

> **提示：** 本次更新无破坏性变更，可直接覆盖安装。

#### 新增：隧道配置页面独立 Activity 与三卡片式布局

- 隧道配置从对话框升级为独立 Activity，支持已发布应用程序路由、WARP 路由、高级设置-源站请求三大功能卡片
- 已发布应用程序路由改为 RecyclerView 列表展示，每条规则可独立编辑/删除，自动维护 catch-all 兜底规则
- 新增高级设置折叠面板，完整支持 16 个 originRequest 字段（超时、TLS、HTTP 头部等），支持全局与单规则两级配置

#### 新增：私有网络路由管理

- 新增网络路由管理卡片，支持 CIDR 网段路由与主机名路由两种类型
- CIDR 路由：支持添加/删除 IP 网段（如 10.0.0.0/8），通过 Zero Trust Teamnet Routes API 管理
- 主机名路由：支持添加/删除私有域名（如 wiki.internal.local），通过 Hostname Routes API 管理
- 路由类型通过 Chip 切换，各自独立格式校验，列表项显示类型标签一目了然

#### 新增：隧道令牌刷新与配置命令增强

- 隧道详情与运行命令对话框新增刷新令牌按钮，一键失效当前令牌并生成新令牌
- 新增 YAML 配置动态生成功能，根据云端实际 ingress 路由自动生成完整配置文件（含 warp-routing、originRequest 等）
- 运行命令新增"配置命令"模式，提供基于 config.yml 的 cloudflared tunnel run 命令
- 显示/复制按钮文案根据当前模式智能切换（令牌模式/配置模式）

本次更新包含隧道配置页面重构、私有网络路由管理、令牌刷新与 YAML 动态生成、规则编辑体验优化及多项质量修复
