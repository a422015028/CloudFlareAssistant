### **v7.7.1 更新日志**

#### 优化：移除全局 Zone ID 绑定，改为自动匹配
- 删除账号编辑页中的「活动域名」切换功能，不再需要手动绑定默认域名
- Pages、R2、路由等所有功能运行时根据 hostname 自动匹配对应 Zone，最长后缀匹配确保准确性
- 标题栏简化为仅显示账号名，不再展示当前选中域名

#### 新增：域名分析页面
- 原仪表盘概览更名为「域名分析」，移入域名详情页功能列表（位于 DNS 记录上方）
- 每个域名独立查看请求量、带宽、缓存命中率、威胁拦截等分析数据
- 支持时间范围切换，与域名上下文深度整合

#### 优化：路由页面支持域名模式
- 路由功能从首页功能区移入域名详情页，按域名维度管理更直观
- 进入路由页面时自动匹配当前域名，无需手动选择或切换
- 保留原账号级入口的浏览能力，双模式无缝适配

#### 优化：存储监控统一归集
- D1 数据库、R2 对象存储、KV 命名空间监控指标从域名分析移至账户分析概览底部
- 统一存储监控数据结构与展示样式，账号级视角一目了然
- 关闭账户分析开关时，首页功能卡片自动适配填满剩余屏幕，下方无留白

#### 重构：Zone ID 转发逻辑
- 重构 R2、路由、DNS 等核心模块的 Zone ID 传递链路，消除冗余参数
- 统一基于 hostname 的自动匹配算法，减少手动配置出错概率
- 简化账号编辑页面，移除旧版域名管理相关 UI 与逻辑

本次更新聚焦架构简化与体验优化，建议所有用户升级。
