### **v7.6.6 更新日志**

#### 新增：Zero Trust 网关位置 DNS 端点配置与展示
- 新增网关位置 DNS 端点配置界面，支持设置端点状态与备用 IPv4
- 网关列表项新增 DNS 端点展示，支持一键复制
- 新增自动获取当前网络出口 IP 并填充 IP 白名单功能
- 扩展 GatewayLocation 数据模型，支持端点状态与备用 IPv4 字段
- 全页面 Snackbar 提示统一替换为 Toast 样式

#### 新增：仪表盘 KV 存储监控
- 仪表盘新增 KV 监控项：读取次数、写入次数、总存储量、命名空间数量
- GraphQL 查询新增 KV 操作与存储统计字段，并按读取/写入分类统计
- 通过 REST API 获取 KV 命名空间统计数据
- 扩展数据模型，支持 KV 监控数据的解析与传递

#### 新增：账户 API 令牌管理
- 新增令牌管理页面，支持用户级与账户级令牌的创建、编辑、删除与查看详情
- 支持令牌验证、权限组选择与 IP 白名单配置
- 新增相关 UI 资源、数据模型与网络请求接口

本次更新聚焦三大功能：网关位置 DNS 端点配置、仪表盘 KV 存储监控与账户 API 令牌管理，建议所有用户升级。
