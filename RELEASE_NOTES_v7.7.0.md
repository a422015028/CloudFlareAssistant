### **v7.7.0 更新日志**

#### 新增：代码片段规则匹配
- 代码片段编辑页新增「规则」配置，支持两种匹配模式：自定义表达式（仅匹配的请求运行片段）与所有格式化请求（全部请求生效）
- 规则与官网 Snippets 控制台完全对齐，基于 Ruleset Engine 表达式语言，每个片段关联一条执行规则

#### 新增：可视化表达式构建器
- 条件构建器支持字段/运算符/值三级联动，涵盖 URI 路径、客户端 IP、国家、HTTP 方法、SSL、UA、Referer、Cookie、请求头等常用字段
- 运算符覆盖等于/不等于、包含、前后缀匹配、正则 matches、集合 in、IP 网段与布尔判断，多条件支持 AND/OR 组合并自动加括号
- 构建器与原始表达式编辑器双视图切换，实时预览生成结果与字符计数，表达式上限 4000 字符与官网一致
- 已有规则自动解析回填构建器，复杂表达式（嵌套/not/函数）无缝回退到编辑器模式

#### 新增：规则状态管理与容错
- 片段列表显示规则状态：所有请求、表达式摘要、已禁用、未关联规则一目了然
- 保存采用合并语义，仅更新目标片段规则，不影响同域名其他片段；删除片段时同步清理规则避免孤儿数据
- Snippets API 报错中文化（未开通权限、名称格式等），首次无规则集自动按空处理，新建名称前置校验小写字母/数字/下划线

本次更新聚焦代码片段规则匹配功能上线，建议所有用户升级。
