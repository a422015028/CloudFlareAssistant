### **v7.7.4 更新日志**

#### 新增：Material You 动态主题
- 支持跟随系统壁纸自动取色，主题色随壁纸变化
- 新增 ThemeHelper.kt 统一管理主题模式与动态配色偏好
- 应用启动时自动应用保存的主题设置
- 不支持动态配色的设备自动隐藏相关选项

#### 新增：统一设置入口
- 首页「显示大小」卡片升级为「设置」卡片，齿轮图标更直观
- 点击打开统一设置对话框，集中管理主题模式、动态配色、显示大小
- 主题模式支持跟随系统 / 浅色 / 深色三选一
- 主题与动态配色切换后自动重启页面即时生效

#### 新增：安全导航扩展
- 新增 NavigationExt.kt，提供 NavController.safeNavigate() 扩展函数
- 仅捕获导航目标找不到的异常，避免延迟导航导致崩溃
- 首页所有卡片跳转已替换为 safeNavigate，其他页面可直接调用

#### 优化：colors.xml 语义化重构
- 重组为状态色、兼容色、基础色三个分类
- 状态色（success / warning / error / neutral / info）不随动态主题变化，保持功能语义一致
- 每个状态色配套容器色（_container）
- 夜间模式补全所有语义色定义
- 旧颜色名保留不删，向后兼容

本次更新聚焦 Material You 动态主题改造与体验优化，建议所有用户升级。
