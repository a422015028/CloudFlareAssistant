### **v7.7.8 更新日志**

> **重要提示：** 本次更新涉及数据库结构升级，安装前请务必备份账号数据，安装完成后需清除 APP 所有数据后重新登录/导入备份配置。

#### 新增：模板商店

- 主页新增模板商店入口卡片，点击进入模板浏览与一键部署页面
- 新增模板商店完整页面体系：模板列表、模板详情、收藏管理、部署配置对话框
- 支持模板分类浏览、关键词搜索、收藏夹管理，收藏数据本地持久化
- 模板卡片展示类型徽标（KV / D1 / R2 / AI / 变量）、版本号、GitHub 跳转入口，支持中英文双语
- 模板详情页支持 Markdown 完整渲染（marked.js + highlight.js），包含代码高亮与自动边距适配
- 实现模板数据源管理，支持远程模板仓库拉取、本地缓存与增量更新

#### 新增：Worker ZIP 多文件部署

- Worker 脚本上传支持 `.zip` 格式，自动解压并检测入口文件，实现多模块部署
- 新增多文件上传 API 接口与 multipart metadata 构建逻辑，兼容 Cloudflare Workers Modules 格式
- ZIP 模式下跳过 Node.js 内容检测，直接使用用户配置的兼容性标志，避免二次上传
- 模板商店部署多文件模板时自动走 ZIP 部署链路，支持包含多个 JS/TS 模块的 Worker 项目

#### 新增：R2 存储桶自动创建与绑定

- 模板部署流程中新增 R2 存储桶自动创建能力，按模板配置自动创建并绑定到 Worker/Pages
- 自动检测同名已有存储桶，支持复用模式避免重复创建
- 绑定配置保留 baseline 已有字段，避免 PATCH 时意外清空其他绑定

#### 新增：Pages 生产环境绑定配置

- 模板商店 Pages 模板部署完成后，自动调用 Pages Project Settings API 配置生产环境绑定
- 支持 KV 命名空间、D1 数据库、R2 存储桶、环境变量与 Secrets 全类型绑定
- 复用现有资源解析与创建逻辑，Worker / Pages 两侧共享资源创建与复用策略
- 部署失败时支持资源回滚，自动清理部署过程中新建的资源

#### 新增：本地数据库升级与迁移

- 新增模板商店相关数据表与本地数据库支持，包含模板元数据、收藏记录、分类索引等
- 数据库版本升级时执行结构迁移，确保新旧版本数据兼容性
- **升级须知：** 因数据库结构变更较大，建议升级前先备份账号配置，安装完成后清除 APP 全部数据并重新登录

#### 优化：日志功能入口与默认状态调整

- 日志功能默认启用状态调整为关闭，减少不必要的资源占用
- 移除首页日志卡片，日志入口迁移至「关于」弹窗，点击 APP 图标即可进入日志页面
- 关于弹窗布局调整，APP 图标增加可点击属性与点击反馈

#### 优化：Pages 子域名与路由规则

- 修复 Pages 域名显示逻辑，统一使用项目配置的 `subdomain` 字段而非硬编码项目名拼接
- 修正 4 处子域名生成代码，移除重复的 `.pages.dev` 后缀拼接，避免生成错误的多级域名
- 新增 `_routes.json` 路由规则自动去重逻辑，修复 Cloudflare 8000057（规则重叠）报错
- 自动生成路由配置时自动合并重叠路径，保留最宽泛的匹配规则

#### 优化：Worker 可观测性设置拆分

- 将可观测性拆分为「日志」与「追踪」两个独立开关，支持分别配置
- 重写 Worker 脚本设置 API 逻辑，分离 traces 和 logs 的独立配置与 PATCH 更新
- Worker 功能开关对话框同步调整为两组独立开关，状态独立回填与保存

#### 优化：模板部署稳定性与错误处理

- 修复部署模板对话框协程作用域问题，增加视图附着检查避免崩溃
- 优化模板部署流程，重上传 / 重部署时保留现有 Worker 配置（绑定、兼容性标志、exports 等），避免被意外覆盖
- 完善错误处理与资源回滚机制，部署失败时按顺序回滚已创建的资源
- 替换多处硬编码文本为正确的字符串资源，统一中英文双语支持
- 更新单元测试以适配新的 API 参数与数据结构

本次更新完成模板商店完整功能、Worker ZIP 多文件部署、Pages 绑定配置、R2 自动创建、数据库升级、日志入口调整与多项稳定性优化，建议所有使用模板部署与多文件 Worker 的用户升级。
