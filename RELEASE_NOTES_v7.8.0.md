### **v7.8.0 更新日志**

> **提示：** 本次更新无破坏性变更，可直接覆盖安装。

#### 新增：Zero Trust 网关 DNS 分析维度扩展

- DNS 分析新增「热门域名」和「命中策略」两个分析维度，查询行为洞察更完整
- 重构分析数据加载逻辑，防止旧请求返回的数据覆盖最新筛选结果
- 新增时间范围与 Top N 选择的持久化，重新进入页面后自动恢复上次筛选条件
- 优化分析页面 UI 布局与交互体验，各区块展示与切换更顺畅

#### 新增：Worker 脚本实时日志页面重构

- WorkerLogsActivity 完全复刻 PagesLogsActivity 的卡片式日志渲染，两个日志页面体验保持一致
- 移除旧的单 TextView 日志展示，改用与 Cloudflare 官网风格一致的事件卡片布局
- 支持按请求 / 事件分组展示日志，点击卡片可展开查看完整 JSON 详情
- 新增单卡片复制功能，可逐条复制事件内容；移除原全局全选复制按钮

#### 新增：独立设置页面与日志代理

- 新增 SettingsActivity 设置页面，支持主题模式、动态配色、显示大小、语言和日志代理配置
- 新增 LogProxyHelper 工具类，实现 WSS 日志连接的代理域名替换
- PagesLogs 与 WorkerLogs 页面统一接入日志代理功能
- 重构 MainActivity，检测到设置变更后自动重建 Activity 以应用新配置

#### 优化：全局卡片与 Toolbar 样式统一

- 统一应用界面的卡片与 Toolbar 背景样式，视觉风格更一致
- 移除布局中硬编码的背景色属性，改为通过代码动态获取主题默认卡片背景色
- 简化 SettingsActivity 的 Toolbar 背景设置逻辑，统一全局卡片背景样式
- 清理冗余的空行和重复代码，优化代码可读性

本次更新完成 Zero Trust DNS 分析维度扩展、Worker 日志页面卡片式重构、独立设置页面与日志代理、全局样式统一及代码清理
