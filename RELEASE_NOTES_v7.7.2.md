### **v7.7.2 更新日志**

#### 新增：代码片段编辑器导入按钮
- 代码片段编辑器新增导入按钮，支持从本地文件快速导入代码片段内容
- 导入后自动填入编辑器，简化手动复制粘贴流程

#### 新增：本地备份功能
- 新增本地存储类型，支持将备份文件保存到应用私有目录或用户自选目录
- 本地备份同样支持密码加密（AES-256-CBC），与 WebDAV、R2 体验一致
- 支持导入外部备份文件，导入后自动加入本地备份列表
- 本地备份列表自动加载，切换到本地 tab 即可查看

#### 新增：备份密码加密
- WebDAV、R2、本地三种存储类型均支持配置备份密码
- 使用 AES-256-CBC 算法加密，PBKDF2 密钥派生，安全可靠
- 加密文件以 `.enc` 后缀标识，自动识别加密/明文文件
- 自动备份统一使用配置密码加密，无需每次手动输入

#### 优化：移除账号 Zone ID 字段冗余
- 移除 `Account.zoneId` 字段相关读写逻辑，改由 Zone 表管理选中分区
- 删除 `AccountDao.updateAccountZoneId` 方法及相关调用链路
- 账号列表与编辑界面不再显示 Zone ID，界面更简洁
- 统一通过 `Zone.isSelected` + ViewModel 运行时状态管理当前选中分区

#### 重构：数据库结构升级
- 新增本地备份配置表与相关 DAO、实体类，持久化本地目录与密码配置
- 为 WebDAV 和 R2 备份配置新增 `backupPassword` 字段
- 更新数据库版本至 9，新增数据库迁移脚本，平滑升级不丢失数据

本次更新聚焦备份能力增强与架构精简，建议所有用户升级。
